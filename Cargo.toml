[package]
name = "telomere"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2.0.11"
bytes = "1.10.0"

[features]
default = ["interpreter", "component-model"]
interpreter = []

component-model = []
