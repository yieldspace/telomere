# Interpreter Baseline Records

No baseline measurement record has been committed yet.

Each accepted first record or intentional comparison update is committed here as
raw JSON named:

```text
YYYY-MM-DD-<short-sha>.json
```

Do not add a placeholder score, manually assembled table, or synthetic JSON.
The harness output is the source of truth; the procedure and publication rules
are in [Interpreter Baseline Methodology](../interpreter-baseline.md).
